# Domain documentation

Skill Manager is a single-context repository.

Before exploring a change, read the root `CONTEXT.md` and relevant records under `docs/adr/` when they exist. If they do not exist, proceed without creating placeholder documentation.

Use the glossary vocabulary from `CONTEXT.md` when it exists. Surface any conflict with an existing ADR rather than silently overriding it.
