## Agent skills

### Issue tracker

Issues live in GitHub for `intelligentrascal/skill-manager`. Use `npx -y gh-axi` for GitHub operations. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the default five-label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. See `docs/agents/domain.md`.
